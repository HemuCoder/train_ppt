# AI Training PPT Web

一个现代化的 AI 培训演示文稿 Web 应用,采用 PPT 风格的交互设计。

## 项目特性

- 📊 **PPT 风格交互** - 键盘导航,全屏演示体验
- 🎨 **现代化设计** - 渐变色彩,流畅动画,响应式布局
- 🏗️ **模块化架构** - 章节独立,样式分离,易于维护
- 🚀 **静态部署** - 纯前端项目,可部署至任何静态托管平台

## 项目结构

```
AI_Training_PPT_Web/
├── src/                    # 源代码目录
│   ├── chapters/          # 章节 HTML 文件
│   ├── styles/            # 样式文件
│   │   ├── base.css      # 基础样式
│   │   └── chapters/     # 章节专属样式
│   ├── layout.html        # 页面布局模板
│   └── script.js          # 交互脚本
├── dist/                   # 构建输出目录 (部署此目录)
├── image/                  # 图片资源
├── tools/                  # 构建工具
│   └── build.py           # Python 构建脚本
└── README.md              # 项目说明
```

## 本地开发

### 构建项目

项目使用 Python 构建脚本合并所有章节和样式:

```bash
# 在项目根目录执行
python3 tools/build.py
```

构建完成后,`dist/` 目录将包含完整的可部署文件。

### 本地预览

使用任意 HTTP 服务器预览:

```bash
# 方式 1: Python 内置服务器
cd dist
python -m http.server 8000

# 方式 2: Node.js http-server (需先安装: npm install -g http-server)
cd dist
http-server -p 8000
```

然后访问 `http://localhost:8000`

## 部署

### Cloudflare Pages 部署

1. **构建配置**:
   - Build command: `python3 tools/build.py`
   - Build output directory: `dist`
   - Root directory: `/`

2. **环境要求**:
   - Python 3.x (Cloudflare Pages 默认支持)

### 其他静态托管平台

本项目可部署至任何支持静态网站的平台:
- Vercel
- Netlify
- GitHub Pages
- AWS S3 + CloudFront

## 使用说明

### 键盘快捷键

- `→` / `Space` - 下一页
- `←` - 上一页
- `Home` - 回到首页
- `End` - 跳到最后一页

### 添加新章节

1. 在 `src/chapters/` 创建新的 HTML 文件 (如 `08_newchapter.html`)
2. 在 `src/styles/chapters/` 创建对应样式文件 (如 `08_newchapter.css`)
3. 运行 `python tools/build.py` 重新构建

## 技术栈

- **HTML5** - 语义化结构
- **CSS3** - 现代样式与动画
- **Vanilla JavaScript** - 轻量级交互
- **Python** - 构建工具

## License

MIT License

---

**Built with ❤️ for AI Training**
